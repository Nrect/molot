# oapi-codegen strict-server config — settlement context.
# Invoked from the repo root by `make openapi`; output is CWD-relative.
package: ports
generate:
  chi-server: true
  strict-server: true
  models: true
  embedded-spec: true
output: internal/settlement/ports/openapi.gen.go
