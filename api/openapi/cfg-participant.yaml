# oapi-codegen strict-server config — participant context.
# Invoked from the repo root by `make openapi`; output is CWD-relative.
package: ports
generate:
  chi-server: true
  strict-server: true
  models: true
  embedded-spec: true
output: internal/participant/ports/openapi.gen.go
