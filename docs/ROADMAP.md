# Roadmap — чем дополнять эталон

> Кандидаты на следующие итерации: что реально используется в продакшен-системах и добавляет учебной ценности. Порядок внутри секций — «дёшево и мощно → дорого». Топ-5 по соотношению ценность/усилия — в конце.

## 1. Быстрые победы (метрики уже собраны, нет только правил)

| # | Что | Зачем | Объём |
|---|---|---|---|
| 1.1 | **Alertmanager + SLO burn-rate алерты** | Определить SLO (напр. 99% ставок < 200ms, 99.9% доставки событий) и multi-window multi-burn-rate правила по SRE book. Метрики (`molot_bus_dead_letter_size`, `molot_settlement_nonterminal_age_seconds`, RED) уже есть — нет ни одного правила | S |
| 1.2 | **Loki** в compose | Централизованные логи + корреляция в Grafana: клик из лога по `trace_id` в трейс Jaeger | S |
| 1.3 | **pprof + Pyroscope** | Continuous profiling — стандарт эксплуатации Go-сервисов | S |

## 2. Тестирование

| # | Что | Зачем | Объём |
|---|---|---|---|
| 2.1 | **k6 load tests** | Готовый доменный сценарий: N виртуальных юзеров бьют ставками в один лот в снайп-окне — проверка optimistic locking под реальной конкуренцией; thresholds (p95, error rate) как CI-гейт | M |
| 2.2 | **Хаос через Toxiproxy** | Прокси между app↔Postgres и app↔PSP: латентность, обрывы посреди саги — проверка crash-seam карты (§6.7 ARCHITECTURE) не только юнитами | M |
| 2.3 | **Property-based** (gopter / rapid) | Инварианты домена: `Money` не теряет минорные единицы; любая последовательность валидных ставок ⇒ leading ≥ runner-up | S |
| 2.4 | **Fuzzing** (go native) | VO-конструкторы и парсинг входа | S |
| 2.5 | **Mutation testing** (gremlins) | Доказывает, что тесты ловят поломки, а не зелёные для галочки | M |

## 3. Supply chain / security

| # | Что | Зачем | Объём |
|---|---|---|---|
| 3.1 | **CI-цепочка безопасности**: govulncheck + gosec + trivy (скан образа) + SBOM (syft) + cosign-подпись + dependabot | Текущий стандарт прода; добавляется быстро | S |
| 3.2 | **Threat model** (STRIDE по контекстам) | Документ-артефакт security-ревью; редко у кого есть | M |
| 3.3 | **OIDC по-настоящему** (Keycloak в compose, JWKS + ротация ключей) | Закрывает задокументированное будущее `AUTH_MODE=jwks` | M |

## 4. Доменная/кодовая логика

| # | Что | Зачем | Объём |
|---|---|---|---|
| 4.1 | **Ledger двойной записи в billing** | Как устроен настоящий биллинг: счёт → проводки (дебет покупателя / кредит продавца / комиссия площадки), инвариант «сумма проводок по транзакции = 0». Сильнейшее доменное усиление | L |
| 4.2 | **Idempotency-Key для HTTP** | Формализовать заголовком-стандартом (сейчас — client-generated UUID в теле) | S |
| 4.3 | **Rate limiting** (token bucket per user) + **circuit breaker** на PSP (gobreaker) | Защита от перегрузки и каскадных отказов | S–M |
| 4.4 | **Consumer-driven contract tests** между контекстами | Изменение события V1 ломает CI у потребителя, а не прод | M |
| 4.5 | **Kafka-транспорт** | Уже спланирован (ADR-0002): замена publisher/subscriber + forwarder, контракты и хендлеры не меняются — демонстрация «transport is a detail» | M |

## 5. Платформа (отдельная итерация)

| # | Что | Зачем | Объём |
|---|---|---|---|
| 5.1 | **Kubernetes**: helm chart, probes (готовы), HPA, PodDisruptionBudget, resource limits + **kind в CI** | Деплой, проверяемый тестом; после этого осмыслен Chaos Mesh вместо Toxiproxy | L |
| 5.2 | **Release-пайплайн**: semver-теги → ghcr-образ → деплой; zero-downtime миграции (expand/contract) | Полный путь до прода | M |
| 5.3 | **Бэкапы с проверкой восстановления** (wal-g + restore-тест в CI) | Все декларируют — никто не тестирует. Очень «взрослый» артефакт | M |

## 6. Реальные деньги — корректность, защита от обмана, стандарты

> Текущее состояние: платёжный **поток** корректен (Money = int64 минорных единиц, идемпотентный Charge/Refund, outbox, FOR UPDATE+version — нет двойных списаний, потерянных событий и lost updates). Но система деньги **не хранит**: нет счетов, балансов, проводок, сверки, выплат. Этот раздел — путь от «деньги проходят сквозь» к «деньгам можно доверять».

### 6.1 Учёт (фундамент)

| # | Что | Зачем | Объём |
|---|---|---|---|
| 6.1.1 | **Ledger двойной записи** (расширение 4.1): append-only журнал проводок «дебет покупателя / кредит продавца / кредит комиссии», инвариант «сумма проводок транзакции = 0» (CHECK + доменный guard), баланс = свёртка проводок, проводки никогда не UPDATE/DELETE — только сторно | Деньги как факты, а не как поля статуса; любой баланс воспроизводим из истории | L |
| 6.1.2 | **Политика округления** — формализовать: banker's rounding для комиссии, явное правило «в чью пользу копейка», property-тест «сумма частей == целому при любом разбиении» | Копейки на миллионах операций = реальные деньги и реальные иски | S |
| 6.1.3 | **Неизменяемый аудит-журнал** финансовых операций: append-only, опционально hash-chain (каждая запись содержит хеш предыдущей) | Доказуемость «никто не подменил историю» для разборов и регуляторики | M |
| 6.1.4 | **Балансы и выплаты продавцам (payouts)**: баланс продавца из ledger, hold-период (защита от chargeback), расписание выплат, идемпотентная выплата через PSP | Деньги должны не только входить, но и выходить — с теми же гарантиями | L |

### 6.2 Интеграция с PSP по-настоящему

| # | Что | Зачем | Объём |
|---|---|---|---|
| 6.2.1 | **Вебхуки PSP**: подтверждение оплаты асинхронное (не в ответе Charge) — подпись вебхука (HMAC), идемпотентная обработка, reconcile статуса до прихода вебхука | Так работают все реальные PSP (Stripe/Adyen/ЮKassa); синхронный Charge — учебное упрощение | M |
| 6.2.2 | **Ежедневная сверка (reconciliation)** с отчётом PSP: наш ledger vs settlement report PSP, расхождение → инцидент-очередь с ручным разбором | Единственный способ ЗАМЕТИТЬ, что систему обманули или PSP ошибся | M |
| 6.2.3 | **Chargeback/dispute поток**: возврат → сторно-проводки в ledger, удержание из баланса продавца, статусная машина диспута | Обязательная ветка реального эквайринга | M |
| 6.2.4 | **Частичные возвраты** и отмена авторизации (auth/capture-модель вместо немедленного charge: hold при ставке-победе → capture при подтверждении) | Стандартная модель карточных платежей | M |

### 6.3 Анти-фрод (чтобы не обманывали)

| # | Что | Зачем | Объём |
|---|---|---|---|
| 6.3.1 | **Shill bidding** (накрутка продавцом через подставных): эвристики — связь продавец↔бидер (IP/device/payment fingerprint), паттерн «ставит, но никогда не выигрывает», флаг в инцидент-очередь | Фрод №1 на аукционах | M |
| 6.3.2 | **Velocity-лимиты**: ставок/мин на юзера, сумм/день, новый аккаунт → жёстче (уже есть verifyAbove — расширить до политики риск-уровней) | Дешёвый барьер для ботов и угнанных аккаунтов | S |
| 6.3.3 | **Идемпотентность как защита от replay**: Idempotency-Key на всех money-эндпоинтах (4.2) + TTL-хранилище ключей с зафиксированным ответом | Повтор запроса (злонамеренный или сетевой) не создаёт второй операции | S |
| 6.3.4 | **Инцидент-очередь / manual review**: подозрительные операции не блокируются молча, а попадают в очередь оператора (контекст ops) с резолюцией и аудитом решения | Антифрод без процесса разбора = просто логи | M |
| 6.3.5 | **4-eyes principle** на критичные ручные операции (выплата вне расписания, ручное сторно): вторая подпись оператора | Защита от инсайдера — стандарт финансовых бэкофисов | M |

### 6.4 Стандарты и регуляторика

| # | Что | Зачем | Объём |
|---|---|---|---|
| 6.4.1 | **ISO 4217** (валюты — уже соблюдено), **ISO 8601/UTC** (уже), задокументировать денежные форматы API (minor units, без float в JSON) | Контракт «как мы передаём деньги» — явный, не по умолчанию | S |
| 6.4.2 | **PCI DSS scope-нота**: карточные данные НЕ касаются системы (токенизация на стороне PSP) — задокументировать границу скоупа, чтобы она не размылась | Случайно начать принимать PAN = катастрофа сертификации | S |
| 6.4.3 | **KYC/AML-каркас**: participant.верификация → уровни (анонимный/базовый/полный), лимиты операций по уровню, санкционный скрининг как порт (адаптер-заглушка) | Площадка с деньгами обязана; в эталоне — каркас с честными границами | M |
| 6.4.4 | **Retention-политика** финансовых записей (хранить N лет, ledger не подпадает под «right to be forgotten» — PII отделена от проводок) | GDPR/152-ФЗ дружат с бухгалтерией только при таком разделении | S |

**Порядок внедрения раздела 6:** 6.1.1 (ledger) → 6.1.2 (округление) → 6.2.1 (вебхуки) → 6.2.2 (сверка) → 6.3.2/6.3.3 (дешёвые барьеры) → остальное. Без ledger остальные пункты строить не на чем.

## 7. Неочевидное и редкое (то, что почти никто не делает)

### 7.1 Тестирование за пределами пирамиды

| # | Что | Зачем | Объём |
|---|---|---|---|
| 7.1.1 | **Deterministic Simulation Testing** (стиль FoundationDB / TigerBeetle): вся система in-memory, фейковое время + псевдослучайный планировщик от seed; тысячи случайных сценариев (ставки, крахи, redelivery, экспирации) за секунды; любой баг воспроизводим по seed. Предпосылки уже есть: clock — интерфейс, in-memory репы, идемпотентная сага | Самый мощный способ найти баги распределённой логики; после него хаос-тесты — игрушка | L |
| 7.1.2 | **TLA+ модель саги settlement**: спека state machine + model checker перебирает ВСЕ интерливинги событий — доказательство «деньги не теряются ни при каком порядке доставки» (так AWS проверяет DynamoDB/S3) | Формальная верификация вместо «тестами вроде покрыли» | M |
| 7.1.3 | **Trace-based testing**: ассерты на структуру OTel-трейса в CI («в PayInvoice спан psp.Charge строго до tx.commit») | Тестируется сам протокол, не только результат; трейсы уже есть | S |
| 7.1.4 | **Linearizability checking** (porcupine): конкурентная история ставок через чекер линеаризуемости | Математическое доказательство честности optimistic locking | M |
| 7.1.5 | **Двухверсионный тест n / n+1**: старый и новый бинарь одновременно на одной БД в CI | Единственное честное доказательство zero-downtime деплоя | M |
| 7.1.6 | **Golden-файлы событий**: JSON-снапшоты всех V1; сегодняшний код обязан читать вчерашние | Append-only контракт событий перестаёт быть честным словом | S |
| 7.1.7 | **Агентная экономическая симуляция**: боты-стратегии (снайпер, инкрементальщик, shill) гоняют тысячи аукционов; ассерты экономических инвариантов (выручка = Σ hammer, нет отрицательных балансов, shill-детектор ловит подсадного) | Стыкуется с DST (7.1.1) и антифродом (6.3) | M |

### 7.2 Доменные фишки аукциона

| # | Что | Зачем | Объём |
|---|---|---|---|
| 7.2.1 | **Commit-reveal для резервной цены**: при листинге публикуется `hash(reserve ‖ salt)`, после закрытия — раскрытие; участник криптографически проверяет, что резерв не двигали по ходу торгов | Честность площадки доказуема, а не декларируема. Уникальная фишка для аукционного домена | M |
| 7.2.2 | **Vickrey-аукцион** (победитель платит вторую цену) как вторая механика | Полиморфизм доменной стратегии: новая механика = новый VO политики, ядро не трогается | M |

### 7.2bis Архитектурная демонстрация

| # | Что | Зачем | Объём |
|---|---|---|---|
| 7.2.3 | **Selective deployment — два бинаря из тех же модулей**: `cmd/monolith` без notification + отдельный `cmd/notifier` только с ним (другая комбинация composition root; шина у них общая — watermill-sql/Kafka) | Полпути к микросервисам без микросервисов: доказывает, что контексты реально независимы — модуль выносится в отдельный процесс пересборкой composition root, без правки домена/app/контрактов. Заодно первый шаг рецепта выноса контекста (фасад → gRPC — следующий) | M |

### 7.3 Эксплуатационные редкости

| # | Что | Зачем | Объём |
|---|---|---|---|
| 7.3.1 | **Scientist-паттерн** (GitHub Scientist): старый и новый код-путь параллельно на проде, сравнение результатов в метрику | Безопасный рефакторинг критичной логики на живом трафике | M |
| 7.3.2 | **Event replay tooling**: пересборка любой проекции с нуля из истории событий одной командой | Outbox уже хранит всё — нужен только инструмент; заодно «what-if» отладка | M |
| 7.3.3 | **Crypto-shredding**: PII под персональным ключом юзера; «забыть юзера» (GDPR) = удалить ключ, append-only ledger не трогается | Решает конфликт «бухгалтерия vs right to be forgotten»; пара к 6.4.4 | M |
| 7.3.4 | **Fitness functions**: архитектурные метрики в CI (% домена без внешних импортов, глубина зависимостей, размер контекстов) с трендом | Деградация архитектуры видна в графике, а не на ревью через год | S |
| 7.3.5 | **Admin TUI** (bubbletea): консоль оператора — разбор dead-letter и requeue | Маленькая вещь, ощущение продукта другое | S |

**Топ-3 раздела по «вау/усилия»:** 7.1.1 (DST) → 7.2.1 (commit-reveal) → 7.1.3 (trace-based, почти бесплатно).

## 8. Продукт вокруг ядра (чего не хватает до реального маркетплейса)

| # | Что | Зачем | Объём |
|---|---|---|---|
| 8.1 | **Realtime: WebSocket/SSE для live-ставок** — цена и таймер обновляются без F5; hub на инстанс + fan-out через шину (событие BidPlacedV1 уже есть), backpressure на медленных клиентов, reconnect с resume | Аукцион без live-обновлений неполноценен; учебно — самая частая realtime-задача прода (рассылка событий тысячам подключений) | L |
| 8.2 | **Поиск каталога**: Postgres FTS для старта → Meilisearch/Elastic как отдельный read store за существующим интерфейсом read model (проекция из событий) | Маркетплейс живёт поиском; демонстрирует «отдельный read store» из правила 32 на деле | M |
| 8.3 | **Медиа лотов**: фото в S3-совместимое хранилище (MinIO в compose), presigned upload URL (файл не проходит через приложение), пайплайн тамбнейлов воркером | Ни один реальный лот не продаётся без фото; presigned-паттерн — стандарт индустрии | M |
| 8.4 | **Реальные каналы нотификаций**: SMTP-провайдер за существующим портом + digest/батчинг, unsubscribe, retry-политика канала | Сейчас фейк-email; порт уже есть — меняется только адаптер (демонстрация гексагональности) | S–M |
| 8.5 | **Аналитика/OLAP**: события → ClickHouse (consumer-проекция), отчёты продавцу (конверсия лотов, динамика цен) | Отдельный аналитический read-path из той же шины; классика event-driven систем | M–L |
| 8.6 | **Бэкофис модерации**: очередь лотов на премодерацию, скрытие лота, бан участника — контекст ops с собственными командами | Любая площадка обязана уметь модерировать; заодно второй потребитель ролей operations | M |
| 8.7 | **API lifecycle**: правила версионирования публичного HTTP (v1 в пути, deprecation-заголовки, sunset-политика) — документ + пример v2-эндпоинта | События версионируются (V1), а HTTP-контракт — ещё нет | S |

## 9. Эксплуатация как процесс (не код, но без этого прод не живёт)

| # | Что | Зачем | Объём |
|---|---|---|---|
| 9.1 | **Incident management**: севериті-уровни, on-call процесс, постмортем-шаблон (blameless), runbook на КАЖДЫЙ алерт (алерт без runbook = шум) | Связывает алерты (1.1) с действиями людей | S |
| 9.2 | **DR-план**: RTO/RPO целеполагание, сценарий «потеряли БД» = restore-тест (5.3) + сценарий «потеряли регион» (осознанно out of scope — задокументировать) | Бэкап без плана восстановления — не DR | S |
| 9.3 | **Capacity planning**: модель нагрузки (ставок/сек на лот × лотов), headroom-политика (50%), связка с k6-результатами (2.1) | «Сколько реплик надо к чёрной пятнице» — расчёт, а не паника | S |
| 9.4 | **Деплой-стратегия**: canary с автооткатом по SLO-метрикам (burn-rate за 10 мин > порога → rollback) — поверх 5.2 | Деплой перестаёт быть событием | M |
| 9.5 | **FinOps-минимум**: затраты по контексту (лейблы ресурсов), cost-метрика рядом с RED на дашборде | Инженер видит цену своего кода; редкая, но взрослая практика | S |

## 10. Команда и долгая поддержка (раз цель — эталон сопровождаемости)

| # | Что | Зачем | Объём |
|---|---|---|---|
| 10.1 | **CONTRIBUTING.md + onboarding-гайд**: как добавить use case / событие / контекст — пошаговый рецепт со ссылками на правила аудита | Новый разработчик продуктивен в день 1; главный тест сопровождаемости | S |
| 10.2 | **CODEOWNERS по контекстам** + PR-шаблон с чек-листом из BOOK_AUDIT §10 | Ревью против контракта становится механикой, а не памятью | S |
| 10.3 | **RFC-процесс**: изменения архитектуры — через короткий RFC-документ в docs/rfc/ до кода (ADR фиксирует решение, RFC — обсуждение) | Так масштабируется принятие решений на команду | S |
| 10.4 | **Дисциплина зависимостей**: renovate с авто-PR, политика «минорные — автомердж по зелёному CI, мажорные — RFC» | Зависимости гниют тихо; долгая поддержка = управляемые обновления | S |

## 11. Безопасность (сводный слой: AppSec + InfraSec + процесс)

> Что уже есть в коде: acting user явным параметром repo (правило 21), авторизация в WHERE-предикатах, анти-enumeration инвойсов (404 вместо 403), JWT-middleware единый, security headers (nosniff, X-Frame-Options), NoCache на /api, параметризованный SQL везде, distroless-образ, секреты только через env. Связанные пункты: 3.1 (supply chain CI), 3.2 (threat model), 3.3 (OIDC), 6.3 (антифрод), 6.4 (комплаенс). Ниже — недостающее.

### 11.1 AppSec — приложение

| # | Что | Зачем | Объём |
|---|---|---|---|
| 11.1.1 | **Харднинг входа**: лимит размера body, лимит глубины/размера JSON, строгая валидация через openapi-схемы (уже частично), reject unknown fields на money-эндпоинтах | Дешёвые DoS- и smuggling-векторы закрываются конфигом | S |
| 11.1.2 | **Токены всерьёз**: короткий TTL access + refresh с ротацией, revocation (logout/компрометация), aud/iss-валидация, kid-ротация ключей | Сейчас один долгоживущий HS256 — нормально для dev, не для прода | M |
| 11.1.3 | **MFA для роли operations** (TOTP) + отдельный аудит каждого действия оператора | Оператор = самый ценный аккаунт для атакующего; пара к 4-eyes (6.3.5) | M |
| 11.1.4 | **Anti-bruteforce**: троттлинг auth-эндпоинтов, защита от credential stuffing (rate limit по IP+аккаунту, capcha-порт как заглушка) | Стыкуется с rate limiting (4.3/HIGHLOAD) | S |
| 11.1.5 | **Лог-гигиена**: scrubbing PII/токенов/ключей в slog-хендлере (deny-list атрибутов), тест «секрет не попадает в лог» | Утечки чаще через логи, чем через БД | S |
| 11.1.6 | **SSRF-дисциплина**: правило «никаких user-provided URL» задокументировано; для будущих вебхуков продавцов — allowlist+запрет приватных диапазонов | Превентивно: сейчас векторов нет, при вебхуках появятся | S |
| 11.1.7 | **CSP + HSTS** на edge (когда появится TLS-терминация/фронт) | Дополнение к уже стоящим nosniff/X-Frame-Options | S |

### 11.2 InfraSec — платформа

| # | Что | Зачем | Объём |
|---|---|---|---|
| 11.2.1 | **TLS везде**: терминация на edge (caddy/traefik в compose — auto-cert), внутренние коннекты PG с sslmode=require; mTLS между сервисами — при выносе контекстов | Сейчас всё plaintext — ок для localhost, недопустимо дальше | M |
| 11.2.2 | **Secrets management**: sops/age для репо-секретов, Vault/k8s-secrets в проде, ротация DATABASE_URL/JWT-ключей без рестарта (reload-порт) | env-plaintext не переживает первый аудит | M |
| 11.2.3 | **Контейнер-харднинг**: non-root user, read-only rootfs, drop capabilities, base image pin по digest | Distroless уже есть — дожать до конца | S |
| 11.2.4 | **Сетевая сегментация**: в compose — отдельные сети (app↔PG, app↔collector; Grafana не видит PG), в k8s — NetworkPolicy deny-by-default + egress-контроль | Скомпрометированный компонент не ходит куда хочет | S–M |
| 11.2.5 | **WAF/edge-фильтр** (coraza/cloud-WAF) перед публичным API | Срезает фоновый шум сканеров до приложения | M |

### 11.3 Процесс безопасности

| # | Что | Зачем | Объём |
|---|---|---|---|
| 11.3.1 | **Security incident runbook** (отдельно от обычных инцидентов): «утёк JWT-секрет» → шаги ротации; «утёк доступ к БД» → шаги; таблица «секрет → как ротировать → blast radius» | Ротация под давлением без runbook = вторая авария | S |
| 11.3.2 | **Repo-гигиена**: branch protection, обязательное ревью, signed commits (опц.), secret scanning (gitleaks в CI + pre-commit) | Полсекрета в истории git = скомпрометирован навсегда | S |
| 11.3.3 | **Go-цепочка поставки**: GOPROXY-политика, `go mod verify` в CI, GOVCS-ограничения; пара к 3.1 (govulncheck/SBOM/cosign) | Dependency confusion / typosquatting | S |
| 11.3.4 | **Pentest-готовность**: scope-документ (что in/out), безопасная тестовая среда с фейковым PSP, баг-баунти-полиси заготовкой | Эталон должен быть готов к тому, что его попробуют сломать | S |

**Порядок:** 11.1.5 + 11.3.2 (дешёвые утечки) → 11.1.2 (токены) → 11.2.1 (TLS) → 11.2.2 (секреты) → 11.1.3 (MFA ops) → остальное. Threat model (3.2) стоит сделать ДО этого раздела — она расставит приоритеты под реальные векторы.

## Топ-5 (ценность / усилия)

1. **1.1** Алерты + SLO burn-rate — дёшево, метрики ждут.
2. **2.1** k6 — конкурентные ставки = идеальный лоад-сценарий.
3. **4.1** Ledger — реальные деньги без него не делают.
4. **3.1** Supply-chain CI — стандарт прода за вечер.
5. **2.2** Toxiproxy-хаос — сага под реальными сбоями.

После топ-5 кодовая часть честно «добавить нечего»; дальше — платформенный слой (раздел 5).
